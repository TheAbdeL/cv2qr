# dependencies
/node_modules

# next.js build output
/.next
/out

# env (secrets) — the .env.local.example file is committed, real values are not
.env
.env*.local

# misc
.DS_Store
npm-debug.log*
*.pem
